BB_CONF_FRAGMENT_SUMMARY = "Remove work directories after a successful build"
BB_CONF_FRAGMENT_DESCRIPTION = "Frees disk space by removing each recipe's WORKDIR once it builds successfully, useful in CI where disk space is limited"

INHERIT += 'rm_work'
