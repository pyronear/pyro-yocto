[Unit]
Description=Persistent home directory for engine
DefaultDependencies=no
Requires=data.mount
After=data.mount systemd-tmpfiles-setup.service
Before=multi-user.target

[Mount]
What=/data/home/engine
Where=/home/engine
Type=none
Options=bind

[Install]
WantedBy=multi-user.target